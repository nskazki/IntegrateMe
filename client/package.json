{
  "name": "integrate-me-client",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run lite-server",
    "lite-server": "lite-server -c lite-server.json"
  },
  "dependencies": {
    "angular": "^1.5.8",
    "angular-resource": "^1.5.8",
    "ngcomponentrouter": "^2.1.0"
  },
  "devDependencies": {
    "lite-server": "^2.2.2"
  }
}
